---
protocol: fcop
version: 1
reporter: ME
severity: low
status: open
summary: "fcop_audit 输出 fcop_rules_version_local: null（redeploy_rules 成功后仍 null）"
created_at: '2026-05-13T09:16:00+08:00'
references:
  - CodeFlow project Mode 1 sprint 2026-05-12 21:50
---

# ISSUE-009: `fcop_audit` `fcop_rules_version_local` 永远 `null`

## 严重性

**P2 low**（不影响 audit 核心功能 / 但 version tracking 失效 / `_local` 字段形同虚设）

## 问题摘要

`fcop_audit(scope='takeover')` 输出 `fcop_rules_version_local: null`，即使 `redeploy_rules()` 已成功部署 `fcop_rules_version_package=2.4.0` 到项目 `.cursor/rules/fcop-rules.mdc`。

## 实证

CodeFlow Mode 1 sprint 2026-05-12 21:50:

```
fcop_rules_version_local: null
fcop_rules_version_package: 2.4.0
```

- `redeploy_rules(force=True)` 成功（14 deployed + 14 archived）
- `.cursor/rules/fcop-rules.mdc` 文件存在 + 内容正确
- 但 `fcop_audit` 读不到 / 或未实现读取逻辑

## 建议 patch

读取 `.cursor/rules/fcop-rules.mdc` frontmatter 中的 version 元数据字段，填入 `fcop_rules_version_local`。

若 frontmatter 中无标准 version 字段，可通过 sentinel marker 或文件头注释提取。
