# 1.0.0 — AI OS Protocol Layer (final, 2026-05-09)

> **Status**: Final release. Ships exactly the protocol surface
> frozen by `1.0.0-rc.1` plus three additive RC-window deliverables
> ([ADR-0022](../../adr/ADR-0022-workspace-directory-convention.md)
> Phase 2 `Project.workspace_dir` refactor, `Project.workspace_layout`
> introspection property, and `docs/MIGRATION-1.0.md` user-facing
> migration guide).
>
> **Promise**: any project that ran on `1.0.0-rc.1` keeps running on
> `1.0.0` with zero changes; any 0.7.x project can opt into v1.0
> with `pip install --upgrade fcop fcop-mcp` + an optional
> `fcop migrate-workspace --apply` run.
>
> **Scope**: 协议本体的 framing 升级 + 7 核心抽象的最小可运行内核
> (POSIX 在 Unix 中的位置；具体兑现是 7 抽象的最小语义冻结，不是
> K8s 级完整 OS)。

## 中文 TL;DR

`fcop@1.0.0` 是把 0.7.x 的"AI 协作规则"完整升级为 **AI OS 协议层 /
Agent Runtime Protocol** 的第一个稳定版。7 抽象（Agent / IPC /
Encoding / Event / Failure / Boundary / Audit）的最小语义合约
**永久冻结**——任何修改协议的变更要求 MAJOR bump 到 v2.x。
0.7.x 文件 100% 兼容；workspace 默认目录从 `docs/agents/` 迁到
顶层 `fcop/`，但提供 `fcop migrate-workspace` 一键迁移工具 +
`Project(workspace_dir="docs/agents")` 永久 escape hatch。

## English TL;DR

`fcop@1.0.0` is the first stable release that promotes 0.7.x's
"AI collaboration rules" into a real **AI OS Protocol Layer /
Agent Runtime Protocol**. The minimum semantic contract of seven
abstractions (Agent / IPC / Encoding / Event / Failure / Boundary /
Audit) is **frozen forever** — any protocol-breaking change
requires a MAJOR bump to v2.x. All 0.7.x files remain 100%
backward compatible. Default workspace directory moves from
`docs/agents/` to top-level `fcop/`, with one-shot
`fcop migrate-workspace` migration tool and a permanent
`Project(workspace_dir="docs/agents")` escape hatch.

## The philosophy 1.0.0 ships with

> 「FCoP 是 agent 的协议，我们发现了他，而不是发明；而正好人类可以读懂。」

v1.0 第一次把这句话从 essay 抬升到协议级宪章——见 [ADR-0015 §FCoP is discovered, not invented](../../adr/ADR-0015-fcop-1.0-ai-os-protocol-charter.md#fcop-is-discovered-not-invented)。

三条对所有后续 v1.x 决策的 tiebreaker：

1. **FCoP belongs to agents, not to us.** 任何候选加进协议的东西必须先答：「agent 在共享文件系统里自由协作时，会自然做这件事吗？」答否就是发明，不属于 FCoP。
2. **Our job is observation + naming + formalization**, not design. 二选一时挑「有更多 field 证据的」，不挑「理论上更漂亮的」。
3. **Human readability is a bonus, not a goal.** Substrate 恰好是 filesystem + Markdown，人能读——这是 FCoP 最重要的副作用；但**为人类可读而设计**会让 FCoP 退化回文档格式。

最强的 field evidence 已经写进 [ADR-0021 §Open Knowledge Surface](../../adr/ADR-0021-encoding-abstraction.md)：真实 FCoP 项目里，agent 在没有任何 spec 指导的情况下自发涌现 5 类命名前缀（`GUIDE-` / `SPEC-` / `STATUS-` / `TEAM-` / `LETTER-`），全部遵守同一 grammar。这件事 v1.0 在协议层正式承认为 Encoding Abstraction 的 **Open Knowledge Surface**——与 IPC Surface 并列的"另一面"。

## What 1.0.0 ships

### 1. Framing 升级 — FCoP 是 AI OS 的 POSIX 层

| 旧 (0.7.x) | 新 (1.0.0) |
|---|---|
| File-based Coordination Protocol | **Agent Runtime Protocol — the AI OS protocol layer** |
| "AI 协作规则" | "AI OS 协议层 / Agent Runtime Protocol" |
| `fcop-mcp` 偶尔被叫 "kernel" | `fcop-mcp` = **Host Adapter**（与 libc 类比）|
| spec 含义模糊 | 5 层架构图 + 7 核心抽象表 + 5 术语永久冻结（见 [ADR-0015 §术语表](../../adr/ADR-0015-fcop-1.0-ai-os-protocol-charter.md)） |

### 2. 7 核心抽象的最小语义冻结

每个抽象由独立 ADR 落地（**全部 Accepted**）：

| # | 抽象 | ADR | 关键产出 |
|---|---|---|---|
| 1 | **Agent**（Lifecycle + 身份） | [ADR-0020](../../adr/ADR-0020-agent-boundary-and-capability.md) | 5 状态枚举 + Boundary 占位 |
| 2 | **Encoding**（filename + frontmatter + 目录） | [ADR-0021](../../adr/ADR-0021-encoding-abstraction.md) | 抽象层 vs Reference Encoding 切开；Markdown 是 v1.0 唯一推荐实现 |
| 3 | **IPC**（Task / Report / Issue / Review envelope） | [ADR-0017](../../adr/ADR-0017-review-file-type-minimal.md) + 现有 | REVIEW 加入 4 类；envelope contract |
| 4 | **Event Model** | [ADR-0018](../../adr/ADR-0018-event-model.md) | 12 事件类型 + `subscribe_events` / `poll_once` contract |
| 5 | **Failure Semantics** | [ADR-0019](../../adr/ADR-0019-failure-and-recovery-semantics.md) | 4 failure × 5 recovery + Session hook |
| 6 | **Boundary**（can / cannot capability） | [ADR-0020](../../adr/ADR-0020-agent-boundary-and-capability.md) | layer 升级为 capability bundle 简写 |
| 7 | **Audit**（REVIEW minimal surface） | [ADR-0017](../../adr/ADR-0017-review-file-type-minimal.md) | 4 decision enum；needs_human / human_approval **不在 v1.0**（推 v1.2） |

### 3. JSON Schema 形式化（7 抽象 × 7 schema）

`spec/schemas/` 落 7 份 JSON Schema 文件作为 single source of truth（[ADR-0016](../../adr/ADR-0016-json-schema-for-7-abstractions.md)）：

```
spec/schemas/
├── agent.schema.json
├── ipc-envelope.schema.json
├── encoding.schema.json
├── event.schema.json
├── failure.schema.json
├── boundary.schema.json
└── review.schema.json
```

`jsonschema>=4.0,<5.0` 进生产依赖；`referencing.Registry` 跨文件 `$ref` 解析就位；wheel 内 `fcop/_data/schemas/` byte-identical 副本守门测试通过。

### 4. 公开 API 1.0 lock + 版本号语义重定义

ADR-0003 留的 0.7→1.0 一次性清理窗口在此兑现。

| 维度 | 0.x 含义 | 1.x 含义 |
|---|---|---|
| MAJOR | N/A | 协议级 breaking + 至少 6 个月共存 + 官方迁移脚本 |
| MINOR | "新功能可能 break" | **只允许 additive**，新字段对旧 consumer 透明 |
| PATCH | "bug fix" | **绝对零行为变化**，仅 bug fix |

`fcop` 与 `fcop-mcp` 继续 lockstep MAJOR（v1.0 起允许 MINOR 漂移以分别迭代）。

### 5. 文档入口重组

| 删除 | 取代 |
|---|---|
| `docs/fcop-standalone.md` + `.en.md` | `docs/getting-started.md`（L0 + L1 唯一入口）|
| `primer/fcop-primer.md` | 同上（合并）|
| `spec/fcop-spec-v1.0.3.md` | `spec/fcop-runtime-protocol-v1.0.md`（标题直接表达 AI OS Protocol layer 身份）|

### 6. Workspace 迁移：`docs/agents/` → `fcop/`（[ADR-0022](../../adr/ADR-0022-workspace-directory-convention.md)）

v1.0 把默认协议工作区从 `docs/agents/` 迁移到顶层 `fcop/`：

```
旧（0.7.x）：                       新（v1.0+）：
project/                           project/
├── src/                           ├── src/
└── docs/                          └── fcop/
    └── agents/                        ├── fcop.json
        ├── fcop.json                  ├── tasks/
        ├── tasks/                     ├── reports/
        └── ...                        ├── issues/
                                       ├── reviews/      ← REVIEW-* 新加
                                       ├── shared/
                                       └── log/
```

**两阶段交付（已全部落地）**：

- **Phase 1** — `fcop migrate-workspace` CLI（TASK-008，`v1.0.0-rc.1` 随发）：dry-run / `--apply` / git-aware（`git mv` 保留历史）/ 幂等 / both-exist ABORT 防呆 / 留痕 `MIGRATED_FROM_DOCS_AGENTS.md` / 顾问扫描 `.gitignore` 等文件中字符串引用
- **Phase 2** — `Project.workspace_dir` refactor（TASK-012，commit `861713b`）：`Project()` 构造接 `workspace_dir=` kwarg + 4 detect 场景（`fcop/` / `docs/agents/` / 都有 / 都无）+ `Project.workspace_layout` introspection property + `LETTER-TO-ADMIN.md` deposit 路径动态化 + MCP server reply 路径动态化

```bash
fcop migrate-workspace               # dry run
fcop migrate-workspace --apply       # 执行（git mv 保留历史）
```

**兼容承诺**：

- 0.7.x 项目不会被破坏，只会得 warning 指向 migrator
- `Project(workspace_dir="docs/agents/")` 显式调用永远合法（escape hatch 永久保留）
- 详见 [`docs/MIGRATION-1.0.md`](../MIGRATION-1.0.md) §2 与 [ADR-0022 §Backwards Compatibility](../../adr/ADR-0022-workspace-directory-convention.md)

## What stays the same

- 所有 0.7.x 的 TASK / REPORT / ISSUE 文件 100% 兼容
- `fcop_report()` 输出格式不变
- `fcop-mcp` 22 个 MCP tool 接口不变（v1.0 新增 4 个：`write_review` / `report_failure` / `recover_session` / `assert_boundary`，但旧 tool 不动）
- `fcop-rules.mdc` / `fcop-protocol.mdc` 顶部 description 升级 framing；`fcop-rules.mdc` 1.9.0 新增 Rule 9（v1.0 4 抽象 additive section），既有 Rule 0..8 不动
- Cursor 用户 mcp.json 配置零修改

## Migration for 0.7.x users

完整指南见 [`docs/MIGRATION-1.0.md`](../MIGRATION-1.0.md)（8 章节，~250 行）：

- §0 心智模型——3 条独立 breaking 维度（framing / workspace / 4 新抽象）
- §1 协议 framing 升级（概念变化，不需改代码）
- §2 workspace 目录迁移 3 选项（A 接受 / B 锁定 legacy / C 自定义路径）+ auto-detect 矩阵 + 完全 rollback
- §3 4 新抽象 walkthrough（REVIEW / Failure / Boundary / Event）
- §4 JSON Schema 集成（给写工具的人）
- §5 6 类常见踩坑（跨版本跳级 / 私有符号 / CI hard-coded paths / monorepo / MCP letter 路径 / RC → final）
- §6 估时表（0 分钟 → 4 小时按用户状态分类）
- §7 进一步阅读 + §8 Where to file issues

## Quality gates (final ship checklist)

- [x] `pytest tests/ -q` — **971 passed, 1 failed, 16 warnings in 37s**
  （1 fail 是 pre-existing date-pinned mcp test，与 1.0 改动无关；
  从 0.7.2 时代的约 600 用例增长到 971）
- [x] `mypy --strict src/fcop` / `src/fcop_mcp` 全绿
- [x] `tests/test_fcop/test_public_surface.py` 快照更新（新增 `Project.subscribe_events / write_review / report_failure / recover_session / assert_boundary / poll_once / workspace_dir kwarg / workspace_layout property` 等）
- [x] `tests/test_fcop_mcp/test_tool_surface.py` 快照含 v1.0 新增 tool
- [x] CHANGELOG.md `[1.0.0-rc.1]` 段 + `[Unreleased]` 段（含 ADR-0022 Phase 2 + MIGRATION-1.0.md）
- [x] `docs/MIGRATION-1.0.md` 存在 + 通过 ReadLints
- [x] ADR-0015 / 0016 / 0017 / 0018 / 0019 / 0020 / 0021 / 0022 全部 Accepted
- [x] `spec/fcop-runtime-protocol-v1.0.md` 落地 + `spec/schemas/*.schema.json` 7 份落地 + wheel 副本守门测试
- [ ] git tag `v1.0.0` + GitHub Release 配本文为 release notes（**待打 final tag**）
- [ ] Zenodo DOI 自动 mint 后 README badge 更新（**Zenodo 处理 GitHub release 后人工 bump**）

## Release commands

```bash
# 1. 把 [Unreleased] 段并入 [1.0.0]（CHANGELOG 收尾）
#    注意 [1.0.0-rc.1] 段保留作为历史；新 [1.0.0] 段记录 RC → final
#    的 delta（即 ADR-0022 Phase 2 + MIGRATION-1.0.md + 本 release notes）

# 2. version bump：0.7.2 -> 1.0.0rc1 -> 1.0.0
#    src/fcop/_version.py / mcp/src/fcop_mcp/_version.py / mcp/pyproject.toml
#    统一为 "1.0.0"，pin 改为 `fcop>=1.0,<2.0`

# 3. 提交 + push
git add -A
git commit -m "release: 1.0.0 — AI OS Protocol Layer (final)"
git push origin main

# 4. annotated tag
git tag -a v1.0.0 -F .git/TAG_MSG_V100.txt   # 见下方 tag message 模板
git push origin v1.0.0

# 5. GitHub Release（本文档作为 notes）
gh release create v1.0.0 \
    --repo joinwell52-AI/FCoP \
    --title "1.0.0 — AI OS Protocol Layer" \
    --notes-file docs/releases/1.0.0.md

# 6. PyPI publish（如果项目走 PyPI 发布；目前 fcop 也走 GitHub release artifacts）
#    python -m build && twine upload dist/*
#    cd mcp && python -m build && twine upload dist/*

# 7. Zenodo 自动 mint DOI（GitHub-Zenodo 集成已配置 per ADR-0014）。
#    DOI 出现后人工 update README badge:
#    sed -i 's/zenodo.NNNNN/<new-doi>/g' README.md README.zh.md
```

### Annotated tag message template (`.git/TAG_MSG_V100.txt`)

```
fcop@1.0.0 — AI OS Protocol Layer (final)

Promotes 0.7.x's "AI collaboration rules" to a real AI OS protocol
layer. The 7 abstractions (Agent / IPC / Encoding / Event / Failure /
Boundary / Audit) minimum semantic contract is frozen for the v1.x
series; any protocol-breaking change requires v2.x.

Highlights vs 0.7.2:
  - Reframed as "Agent Runtime Protocol — the AI OS protocol layer"
    (per ADR-0015)
  - 7 abstractions × 7 JSON Schemas, machine-readable single source
    of truth (per ADR-0016)
  - REVIEW (4th IPC envelope, ADR-0017) + Failure / Recovery
    (ADR-0019) + Boundary / Capability (ADR-0020) + Event Model
    (ADR-0018) all wired with reference impl + tests
  - Encoding abstraction split: protocol-level vs reference encoding
    (ADR-0021); Open Knowledge Surface (5 organic prefix families
    discovered in field) acknowledged
  - Default workspace directory moves docs/agents/ -> fcop/
    (ADR-0022); ships fcop migrate-workspace one-shot CLI; legacy
    layout remains supported via Project(workspace_dir="docs/agents")
    permanent escape hatch
  - Public API surface 1.0 lock; SemVer semantics tightened (MINOR
    additive-only, PATCH zero behaviour change)

Backward compatibility:
  - All 0.7.x TASK / REPORT / ISSUE files remain 100% readable
  - 0.7.x library callers see deprecation warnings (not errors) for
    the workspace path change
  - fcop-mcp 22 existing MCP tools unchanged; 4 new tools added

Migration: docs/MIGRATION-1.0.md (8 sections, walks through framing,
workspace, 4 new abstractions, schema integration, 6 common
pitfalls, time estimates, further reading).

Tests: 971 passed (up from ~600 at 0.7.2). Test suite covers all 7
abstractions end-to-end.

Spec: spec/fcop-runtime-protocol-v1.0.md (normative).
DOI: see Zenodo badge in README (auto-minted post-release).
```

## Post-release observation

- [ ] `pipx install fcop-mcp==1.0.0` 干净安装通过
- [ ] CodeFlow / Bridgeflow 升级 `fcop>=1.0,<2.0` 并 mirror schema
- [ ] PyPI 下载首周曲线
- [ ] [Issue #2](https://github.com/joinwell52-AI/FCoP/issues/2) 上更新进度 comment（"v1.0.0 已发"——RC comment 已发布于 [#2 #4412811192](https://github.com/joinwell52-AI/FCoP/issues/2#issuecomment-4412811192)，final 后追评一次）
- [ ] essay 候选选题："FCoP 1.0：从 AI 协作规则升级为 AI OS 协议层"
- [ ] `MIGRATION-1.0.md` 用户反馈窗口（PR / issue 收集 1-2 周后整理 v1.0.1 doc patch）

## Roadmap pointer

→ 接下来 [`1.1.0.md`](./1.1.0.md)（如存在）：补 Issue #2 延后字段（`risk_level` + `Skill`）、Encoding alternative（JSON / SQLite）、Layer 1 / Layer 3 拆分等。
